__pycache__/
*.pyc
*.pyo
*.swp

# CACTI runtime scratch (input configs + output reports generated per invocation)
cacti/self_gen/

# CACTI build artifacts (re-generated by `make` inside cacti/; the pre-built
# optimized binary `cacti/cacti` is committed for zero-build clones).
cacti/obj_dbg/
cacti/obj_opt/
cacti/cache.cfg_temp
cacti/cache_*.cfg
cacti/cache_*.cfg.out
cacti/*.o
